name: ci
on:
  push:
    branches:
      - main
permissions:
  contents: write
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Upgrade pip and install dependencies
        run: |
          python -m venv env
          source env/bin/activate
          python -m pip install --upgrade pip
          # Instala versiones específicas para evitar problemas de compatibilidad
          pip install mkdocs==1.4.3 mkdocs-material==9.1.16
          pip install mkdocs-static-i18n==1.2.3
          pip install transifex-client
          # Verifica que el plugin se haya instalado correctamente
          pip show mkdocs-static-i18n

      - name: Extract translations
        run: |
          source env/bin/activate
          # Ejecución de MkDocs con el plugin i18n
          mkdocs build --clean
          # Esto debería crear los archivos .pot en la carpeta 'locales'

      - name: Prepare Transifex configuration
        run: |
          tx init --skipsetup
          echo "[main]\nhost = https://www.transifex.com\n" > ~/.transifexrc
          # Configura el archivo Transifex
          echo -e "[geonode_cloud_documentation]\nlang_map = es:es,en:en\ntype = PO" > .tx/config

      - name: Push source content to Transifex
        run: tx push -s  # Subir archivos .pot a Transifex

      - name: Pull translations from Transifex
        run: tx pull -a  # Descargar todas las traducciones

      - name: Build translated site
        run: |
          mkdocs build --clean  # Construir el sitio con las traducciones

      - name: Deploy to GitHub Pages
        run: mkdocs gh-deploy --force
